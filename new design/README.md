# Flash UI export

Open any HTML file directly in a modern browser. No install or build step is required.

The files are the same normalized artifacts used by Flash UI preview and HTML download.
See manifest.json for the selected viewport, fidelity warnings, byte length, and SHA-256 checksum of each file.